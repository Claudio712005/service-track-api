apiVersion: v1
kind: Service
metadata:
  name: service-track-app-lb
  namespace: service-track
  labels:
    app: service-track-app
spec:
  type: LoadBalancer
  selector:
    app: service-track-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
