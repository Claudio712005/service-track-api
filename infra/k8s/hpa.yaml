apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: service-track-app-hpa
  namespace: service-track
spec:
  scaleTargetRef:
    kind: Deployment
    name: service-track-app
    apiVersion: apps/v1
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80